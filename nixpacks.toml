[phases.install]
cmds = ["cd backend && npm install --legacy-peer-deps"]

[phases.build]
cmds = ["cd backend && npx prisma generate && npx prisma migrate deploy && npx nest build"]

[start]
cmd = "cd backend && npm run start:prod"
